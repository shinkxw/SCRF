给定一个代码块及对应的输入范围及采集预期时间max_t
进行三次随机值测试, 平均后得到单次测试时间t(若单次超过max_t则直接返回)
计算所有输入值的组合数量all_n,及所有输入端点的组合数量node_n
max_t/t 得到可进行的测试次数can_n

can_n >= all_n 采集所有输入情况的数据
all_n > can_n >= node_n 优先采集各种端点情况下的数据, 然后根据剩下的次数进行渐进采集
node_n > can_n 报告采集时间不足